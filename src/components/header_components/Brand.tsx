import { StyleSheet, View } from "react-native";

function Brand() {

  const styles = StyleSheet.create({
    container: {
      width: 200,
      height: 50,
      backgroundColor: "#25a4bbff"
}
  })

  return (
    <View style={styles.container}>

    </View>
  )
}

export default Brand;
