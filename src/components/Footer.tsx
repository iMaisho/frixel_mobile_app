import { StyleSheet, View } from "react-native";

function Footer() {

  const styles = StyleSheet.create({
    container: {
      height: 100,
      backgroundColor: "#8989daff"
}
  })

  return (
    <View style={styles.container}>

    </View>
  )
}

export default Footer;
